# NeonBotChat 多账号登录功能设计方案

## 核心要点

### 1. 登录页面设计
- **页面结构**：登录页面 + 已保存账号列表 + 新账号表单
- **交互流程**：
  - 访问 WebUI 时显示登录页面
  - 显示已保存的账号头像列表，点击快速登录
  - 或输入新的 AppID/Secret 登录
  - 可选择"保存账号"以便下次快速登录

### 2. 账号管理 UI
- **位置**：侧边栏左上角，机器人头像下方
- **添加账号**：点击"+"图标，弹出浮窗输入 AppID/Secret
- **切换账号**：点击当前账号头像，显示账号切换下拉菜单
- **删除账号**：悬停账号项显示删除按钮，点击确认后删除

### 3. 消息通知气泡
- **触发条件**：收到新消息 + 不是当前查看的会话
- **动画效果**：
  - 播放提示音（notification.mp3）
  - 头像旁弹出气泡（从右向左滑入）
  - 显示：账号名称、发送者头像、发送者昵称、消息预览
  - 3秒后自动收回（向右滑出动画）
- **样式**：毛玻璃效果，圆角，阴影

### 4. 统计页面增强
- **过滤器**：下拉选择"全部账号"或单个账号
- **数据查询**：支持按 account_id 过滤统计数据
- **UI**：保持现有样式，增加过滤器组件

## 技术实现要点

### 数据库改造
1. 新增 `accounts` 表存储账号信息
2. `conversations` 和 `messages` 表增加 `account_id` 字段
3. 相关查询函数支持 `account_id` 过滤

### 后端 API
1. `/api/accounts/login` - 登录账号
2. `/api/accounts` - 获取所有账号
3. `/api/accounts/{appid}` - 删除账号
4. `/api/accounts/{appid}/switch` - 切换账号
5. `/api/accounts/current` - 获取当前账号
6. `/api/stats/multi` - 多账号统计

### 前端组件
1. `LoginManager` - 登录页面管理
2. `AccountManager` - 账号管理逻辑
3. `NotificationManager` - 消息通知管理

### 文件清单
- **新增文件**：
  - `web/login.html` - 登录页面
  - `web/js/accounts.js` - 账号管理逻辑
  - `web/js/notifications.js` - 消息通知逻辑
  - `web/css/accounts.css` - 账号相关样式
  - `web/sounds/notification.mp3` - 提示音文件
  - `bot_manager.py` - Bot 实例管理器

- **修改文件**：
  - `database.py` - 数据库结构改造
  - `web_server.py` - 添加账号管理 API
  - `init.py` - 支持多账号启动
  - `web/index.html` - 添加账号管理 UI
  - `web/js/core.js` - 集成账号切换逻辑
  - `web/css/components.css` - 添加通知气泡样式

## 实现步骤

### 阶段一：数据库改造
1. 修改 `database.py`，添加 accounts 表
2. 修改 conversations 和 messages 表，增加 account_id 字段
3. 添加数据迁移脚本

### 阶段二：后端改造
1. 创建 `bot_manager.py`，管理多个 Bot 实例
2. 修改 `init.py`，支持多账号启动
3. 修改 `web_server.py`，添加账号管理 API

### 阶段三：前端改造
1. 创建登录页面
2. 修改 index.html，添加账号管理 UI
3. 创建账号管理和通知组件
4. 添加相关 CSS 样式

### 阶段四：测试与优化
1. 测试多账号登录流程
2. 测试账号切换功能
3. 测试消息通知效果
4. 测试统计过滤功能

## 设计优势

1. **用户体验**：登录页面简洁，账号切换方便
2. **功能完整**：支持添加、切换、删除账号
3. **通知效果**：气泡动画 + 提示音，提醒明显
4. **统计灵活**：支持按账号过滤，数据清晰
5. **易于扩展**：架构清晰，便于后续功能扩展

## 注意事项

1. **安全性**：AppSecret 需要加密存储
2. **性能**：多 Bot 实例会占用更多内存
3. **兼容性**：保持现有功能不受影响
4. **错误处理**：添加完善的错误处理和用户提示

---

**文档版本**: v1.0  
**创建日期**: 2026-09-18
