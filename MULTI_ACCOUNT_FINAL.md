# 多账号 WebUI 最终修改说明

## 提交记录复核

此前的 `cef7a44` 已加入多账号相关文件，但页面未加载 `accounts.css`，添加按钮引用了不存在的 `icons/add.svg`，浮窗只修改 `display` 而没有进入现有 `.modal-overlay.show` 状态；`login.html` 也没有后端入口。通知脚本未接入可靠的消息事件，提示音目录未挂载，统计接口则始终返回全部数据。

## 本次完成内容

- 首次没有已保存账号时展示 AppID / AppSecret 登录页；保留 `/login` 入口。
- 账号头像下方增加灰色圆形 `+`，点击打开与现有 WebUI 一致的毛玻璃登录浮窗。
- 点击头像打开账号切换面板，可切换或删除账号；切换后刷新该账号的会话。
- 主页增加“全部账号 / 单个账号”统计范围选择器，统计 API 与周报导出同步使用当前范围。
- 消息与会话写入真实 `account_id`，旧单账号未归属数据自动绑定首个账号。
- 非当前账号收到消息时播放提示音，并从左侧账号栏旁滑出提醒；显示账号、发送者头像和昵称，3 秒后收回。当前账号不弹跨账号提醒。
- 主动发送凭据随账号切换，访问令牌缓存按账号重置，避免复用其他账号令牌。
- 账号 API 不再向浏览器返回 Secret。
- 使用 `uvicorn[standard]` 提供 WebSocket 协议实现，消除 unsupported upgrade / missing WebSocket library 告警。

## 验证

- Python：`py_compile` 通过。
- JavaScript：`node --check` 通过。
- SQLite：全部账号与两个单账号统计断言通过，会话按账号过滤通过。
- FastAPI 路由：首登页、主页面、账号脱敏、统计筛选与提示音资源断言通过。
- 浏览器：已检查主页灰色 `+`、统计范围选择器和现有深色 WebUI 风格。
