# NeonBotChat v28.8.2 更新日志

## 🆕 新功能

### 会话管理
- **置顶会话**：右键会话 → 置顶 / 取消置顶，置顶会话主题色高亮 + 左侧竖条，排序最前
- **消息免打扰**：右键会话 → 免打扰 / 取消，开启后未读气泡变灰
- **不显示会话**：右键会话 → 隐藏 / 显示，隐藏后不在列表出现；**搜索备注名 / 群 OpenID 找回**，点开自动取消隐藏
- **@ 提醒徽标**：被 @ 时会话名旁显示红色 @ 标记，点开清除
- **群信息**：群聊设置顶部显示群 OpenID，一键复制

### 查找聊天记录
- 群聊设置 → 查找聊天记录：搜索**当前群**消息，匹配消息内容 / 发送人昵称 / 附件文件名
- 结果带用户头像（Bot 消息显示自定义头像，未设置回退搜索图标），点击**跳转定位并高亮**原消息

### 桌面通知
- 设置 → 通用 → 桌面通知开关（Switch 样式）
- 新消息系统通知（正在看的会话不打扰）；**@我 强提醒**，点击通知直达会话

### 图片 / 视频灯箱
- 点击图片 / 视频全屏预览：毛玻璃遮罩、大图居中
- 图片视频**混排翻页**：按钮 / 键盘 ←→ / 移动端滑动，边界提示「已是第一张 / 已是最后一张」
- **缩放**：桌面滚轮、移动端双指捏合，中央毛玻璃提示实时缩放比例（50%~400%）
- 消息内视频只显示封面首帧 + 圆形播放按钮，点击进灯箱播放（杜绝双播放）

### 其他
- **消息日期分隔条**：今天 / 昨天 / 8月3日 周一，按天分隔
- **设置 → 日志页**：服务端控制台日志实时同步（最近 500 条，2 秒刷新，级别着色）
- **Toast 重构**：居中方形毛玻璃卡片，图标上文字下，多提示**队列串行**显示
- **消息布局重构**：头像 / 昵称 / 群角色徽章移至气泡外，气泡内仅内容与引用
- **纯媒体免气泡**：图片 / 视频 / 语音消息不垫气泡底

## 🔧 改进

- 收到消息日志精简为单行格式：`收到来自 群OpenID 的 名(OpenID) 的消息: 内容`
- 撤回按钮按 QQ 时限自动隐藏（自己发的、2 分钟内）
- 昵称统一灰色（深浅主题适配），群角色徽章深浅主题独立配色、不透明度提高
- 设置弹窗拉宽至 720px、固定高度、毛玻璃效果修复
- 消息过期文案统一：图片已过期 / 视频已过期 / 语音已过期
- 全站 emoji 图标替换为 SVG：ok / favourite / warning / close / bot / search / up 等
- 常用语功能开发后按需求移除
- 版本号 v26.8.0 → v28.8.2

## 🐛 修复

- 设置弹窗毛玻璃不生效（遮罩过深 + 背景过实）
- 浅色模式 Switch 开关开启态仍是灰色（CSS 特异性冲突）
- 灯箱视频被遮罩盖住（视频缺 position）
- 气泡内视频点击导致双播放（改封面式）
- 灯箱点击视频定位到第一张（closest 向上查找不到子元素 + URL 重定向导致匹配失败，改元素引用定位）
- 图片框残留半透明垫底（media-only 下清理 placeholder 背景与边距）
- 查找聊天记录中 Bot 名字仍显示 🤖 emoji（补 fmtBotMarker 处理）

## 🔄 底层变更

- `database.py`：conversations 表新增 `pinned / muted / hidden` 列（旧库自动迁移）；`search_messages` 支持按会话过滤 + 匹配发送人 / 附件名；新增 `get_messages_around` 定位加载
- `web_server.py`：`/api/search`（支持 `conv_id`）、`/api/logs`、`/api/messages` 支持 `around`；PATCH 会话接口支持 pinned / muted / hidden
- `LogBuffer.py`（新）：root logger 环形缓冲，捕获全部日志（INFO+），保留 500 条
- `init.py`：挂载日志缓冲；uvicorn `log_config=None` 避免 dictConfig 清空 handler；`[NeonBot]` 日志改为 logger 输出；推送消息附带 `conversation_hidden`
- `PatchMsg.py`：收到消息日志精简为单行
- 新增图标：`ok.svg / warning.svg / favourite.svg / close.svg / donotdisturb.svg / disturb.svg / hidden.svg / show.svg`
